# SmartBuilders Portal

## Development

Install dependencies with 
```sh
pnpm install
```

Start a development server:

```sh
pnpm run dev

# or start the server and open the app in a new browser tab
pnpm run dev -- --open
```
